import 'package:flutter/material.dart';

ButtonStyle appButton() {
  return ElevatedButton.styleFrom(
    // minimumSize: Size(5, 7),
    fixedSize: Size(5, 10),
    backgroundColor: Colors.white10
  );
}
